/* 
ЗАДАЧА: удалить все дубликаты из списка почтовой рассылки используя двусвязный список как модель
ДЛЯ ЭТОГО НУЖНЫ:
- отдельный класс с методами для двусвязного списка с абстракцией "позиция"
- отдельный класс для списка почтовой рассылки (и вот конкретно к ним применять интерфейс совпадения)

ВСПОМОГАТЕЛЬНЫЕ МЕТОДЫ:
- метод для обновления позиций 2х узлов в большом списке (чтобы после удаления дубликата связать узлы перед и после дубликата)
- метод который позволит нам идти по позициям а не по нодам потому что так удобнее


ВОПРОСЫ:
1) можно ли использовать интерфейс для совпадения? как мы делали в прошлом году
 (где для каждого типа данных "равно" по-разному проверялось)
 и тогда можно будет написать булевый метод, сравнивающий значения, хранящиеся в 2х разных узлах

2) Как лучше хранить последний элемент списка? (это tail?)
3) нужно ли превращать символьные массивы в строки?

ОБЩИЕ ПОИНТЫ:
валидацию на размер списка не нужно
нужно проверять позицию на существование в списке.
выбрасывать исключения можно когда угодно, но лучше там, где это не критично, делать просто сообщение.
позиция может  не существовать если позиция была например передана для удаления.
для "Равно" использовать посимвольное сравнение (хжширование не надо)


позиция после псоследнего это тто что проверяется первым дело мдля нашего кода.
проверка позиции на существование это мы делаем последним делом потому что это самая трудоёёмкая операция.

DoubleLibnked: 
End(L) -- null
Нет никакого списка пустых

Cursors: 
End(L) -- "-1"
До последнего заполненного мы добираемся перебором от начала и до конца (отдельный метод)
Объект -- позиция точки входа в список
+ точка входа в список пустых
массив узлов статический потому что это для всего.
Инсерт и Делит --- это по сути просто перенос из списка в список 
нужен метод для того чтобы находить последний заполненный элемент
 для того чтобы вставлять в позицию после последнего (добавление элемента в список)



одно пространство имен в котором 2 класса, отдельно класс списка отдельно класс позиции 

лучше переопределить готовый метод equals
если я реализую "равно" в классе объектов и подключу его к классу реализации Locate/
*/


                                                                    /*   ЗАНЯТИЕ 18.02
                                                                     
1. Открытый метод должен правильно работать при любых обстоятельствах (проверки)
        \/
2. Не надо вызывать один открытый метод в другом открытом методе (из-за большого количества проверок

-----------------------------------------------------------------------------------------------------------


1. Чтобы улучшить реализацию двух похожих открытых методов, мы реализуем закрытый метод 
(можем вызывать только мы). Этот метод должен решать поставленную задачу (функциональная прочность метода)
                \/
2. В методах открытых обязательно проверяем возможность вызова закрытого метода, и если можно то вызываем. Иначе не вызываем.
 
  
                                                            КОНКРЕТНО ПО МЕТОДАМ

Insert Delete 
Первым делом проверяем позицию после последнего (это легко проверяется, сравнением позиции р с ппп)

Insert:
а) Список пустой - вставляем новый элемент (он становится головой)
б) Проходим до последнего элемента списка (Last) он возвращает позицию последнего элемента

Delete
а) Если р ппп то ничего делать не нужно, можно просто предупреждение 



Вторым делом смотрим позиция р - голова или хвост? (говорим про курсоры конкретно)
Insert:
а) Если мы вставляем в голову - вставляем новый элемент после головы
Значение из головы копируем в этот новый элемент (только что вставленный)
А в значение головы заносим то, что у нас передаётся в качестве параметра 

Delete:
а) Сдвинуть позицию головы на следующий элемент.


ТРЕТЬИМ делом проверяем, что позиция где-то в середине списка  (для вставки предыдущий не нужен, только для удаления он нужен)


почему для удаления
(тут можем найти элемент в позиции р)

нам потребуется для этого вспомогательный метод, где мы находим предыдущий (GetPrev)
Закрытый метод возвращает позицию предыдущего перед р.  (внутри проверку на голову никакой не нужно)



/*                                                           ЗАНЯТИЕ 25.02
                                                                     
между классами курсоаров и двусвяз мы переключаемся используем using, мейн вообще по минимуму настраивать
нужно держать в голове, что терминальный элемент -- обозначение конца 
как только мы поймали терминальный элемент, все другие проверки БЕССМЫСЛЕННЫ 
терминальный элемент (символ с кодом ноль) не может являться никаким 

переопределить Equals для сравнения между собой созданные объекты  

 */



using ASD.Cursors;
//using ASD.DoubleLinked;

namespace ASD;
public class Program
{
    public static void Main()
    {
        var list = new MyList<MailingList>(); // создаем пустой лист
        MakeList(list);
        list.PrintList(); // вывод
        Console.WriteLine();
        
        DeleteDuplicates(list);
        list.PrintList(); // вывод
    }
    
    private static void MakeList(MyList<MailingList> list)
    {
        // Добавляем адресатов с повторениями
        list.Insert(list.End(), new MailingList("Иванов Иван", "ул. Ленина, 10"));
        list.Insert(list.End(), new MailingList("Петров Петр", "ул. Мира, 15"));
        list.Insert(list.End(), new MailingList("Иванов Иван", "ул. Ленина, 10"));
        list.Insert(list.End(), new MailingList("Сидоров Сидор", "ул. Гагарина, 20"));
        list.Insert(list.End(), new MailingList("Петров Петр", "ул. Мира, 15"));
        list.Insert(list.End(), new MailingList("Васильев Василий", "ул. Пушкина, 5"));
        list.Insert(list.End(), new MailingList("Иванов Иван", "ул. Ленина, 10"));
    }
    
    private static void DeleteDuplicates(MyList<MailingList> list)
    {  
        Position<MailingList> p = list.First(); // получение первой позиции списка
        Position<MailingList> end = list.End(); // получение позиции после последнего
    
        while (!p.Equals(end)) // пока список не кончится
        {
            Position<MailingList> q = list.Next(p); // получаем следующую позицию за проверяемой
            while (!q.Equals(end))
            {
                if (list.Retrieve(p).Equals(list.Retrieve(q))) // если объекты в разных позициях совпали
                {
                    Position<MailingList> deleted = q; // запоминаем удаляемую позицию, чтобы могли перейти к следующей
                    q = list.Next(q); 
                    list.Delete(deleted); // удаляем дубликат
                }
                else
                {
                    q = list.Next(q); // если не удаляли дубликат все равно переходим к следующей
                }
            }
            p = list.Next(p); // ищем дубликаты для объекта на следующей позиции
        }
    }
}